func describeUser(name: String, age: Int, city: String) -> String {
    return "Меня зовут \(name), мне \(age) лет, я живу в городе \(city)."
}
let name = "Кирилл"
var age = 19
let city = "Пермь"

let result = describeUser(name: name, age: age, city: city)
print(result)
